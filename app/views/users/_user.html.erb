<li>
  <%= gravatar_for user, size: Settings.user.gravatar_size_2 %>
  <%= link_to user.name, user %>
  <% if current_user.is_admin? && !user.current_user?(current_user) %>
    | <%= link_to t(".delete"), user, method: :delete,
      data: {confirm: t(".sure")} %>
  <% end %>
</li>
